# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Trash_Where_iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Trash_Where_iOS

  target 'Trash_Where_iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Trash_Where_iOSUITests' do
    # Pods for testing
  end

end
